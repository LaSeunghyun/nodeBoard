import express from "express";
// import * as boardController from "../controller/boardController.js";

const router = express.Router();

// router.post("/", boardController.createBoard);
// router.get("/", boardController.getBoardInfo);
// router.get("/:boardIdx", boardController.getBoardIdInfo);
// router.delete("/:boardIdx", boardController.delBoardIdInfo);
// router.put("/", boardController.putBoardIdInfo);

export default router;
